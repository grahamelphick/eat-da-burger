<img src="https://e7.pngegg.com/pngimages/170/316/png-clipart-hamburger-illustration-hamburger-cheeseburger-veggie-burger-cartoon-burgers-s-food-beef.png" alt="cartoon burger" height="100px">

<h1>Eat-Da-Burger!</h1>

<h2>Burgers to be Devoured:</h2>

<ul>
  {{#each burgers}}
   {{#unless devoured}}
  <li>
    <p>{{this.id}}. {{this.burger_name}}</p>

    <button class="make-devoured" data-id="{{id}}">
      DEVOUR!!
    </button>

  </li>
      {{/unless}}
  {{/each}}
</ul>


<h2>Burgers Eaten:</h2>


<ul>
  {{#each burgers}}
      {{#if devoured}}
  <li>
    <p>{{this.id}}. {{this.burger_name}}</p>
  </li>
      {{/if}}
  {{/each}}
</ul>


<h2>Add a burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <button type="submit" id="submit-btn">Add burger</button>
</form>
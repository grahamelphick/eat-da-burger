<img src="cartoon-burger.png" alt="cartoon burger" height="100px">

<h1>Eat-Da-Burger!</h1>

<h2>Burgers to be Devoured:</h2>

<ul>
  {{#each burgers}}
   {{#unless devoured}}
  <li>
    <p>{{this.id}}. {{this.burger_name}}</p>

    <button class="make-devoured" data-id="{{id}}">
      DEVOUR!!
    </button>

  </li>
      {{/unless}}
  {{/each}}
</ul>

<br>

<h2>Burgers Eaten:</h2>

<ul>
  {{#each burgers}}
      {{#if devoured}}
  <li>
    <p>{{this.id}}. {{this.burger_name}}</p>
  </li>
      {{/if}}
  {{/each}}
</ul>

<br>

<h3>Add a burger</h3>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <button type="submit" id="submit-btn">Add burger</button>
</form>